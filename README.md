# Differential

Internal research environment for applied quantum computing experiments
using IBM Qiskit.

## Setup

```bash
source .venv/bin/activate
pip install -r requirements.txt


---

## 7. Freeze dependencies (important for agents)

Create `requirements.txt`:

```bash
pip freeze > requirements.txt

